- @products.in_groups_of(4).each do |product_row|
  .row
    - product_row.each do |product|
      - if product.present?
        .col-xs-3
          .product_block
            = link_to product_path(product) do
              = image_tag(product.image_ref || 'salme_37_5_road', class: (product.html_class || 'square') + " img-thumbnail")
            %h2= product.name
            %p= product.description
            %p= product.price
            = link_to new_order_path(id: product.id), class: "add" do
              .btn.btn-primary{data: product.id.to_s, name: product.image_ref || 'salme_37_5_road'}
                Tilføj til ordrelisten
                %i.fa.fa-plus

:coffeescript
  $ ->
    order_number = getCookie("order_number")
    if order_number
      order_id = getCookie('order_id')
      $('.add').each (i) ->
        product_id = $(this).attr('href').split('=')[1]
        $(this).attr 'href', '/orders/' + order_id + '/edit?product_id=' + product_id


